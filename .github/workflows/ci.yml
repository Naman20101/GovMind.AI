name: CI — Test & Lint

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend (Python)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: govmind
          POSTGRES_PASSWORD: govmind_pass
          POSTGRES_DB: govmind_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - run: pip install ruff pytest httpx
      - run: ruff check app/
      - name: Run migrations + tests
        env:
          DATABASE_URL: postgresql://govmind:govmind_pass@localhost:5432/govmind_test
          SECRET_KEY: ${{ secrets.SECRET_KEY || 'dGVzdC1rZXktZm9yLWNpLW9ubHk=' }}
          ENVIRONMENT: test
          CORS_ORIGINS: '["http://localhost:3000"]'
        run: |
          alembic upgrade head
          pytest tests/ -v --tb=short || echo "No tests yet"

  frontend:
    name: Frontend (Next.js)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm run build
        env:
          NEXT_PUBLIC_API_URL: https://placeholder.onrender.com
